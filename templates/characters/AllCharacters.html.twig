{% extends 'base.html.twig' %}
{% block title %} Characters {% endblock %}
{% block body %}
    <h1 class="text-center m-5 p-3">Here is displayed all the users</h1>
        <div class="row">
            {% for user in users %}
                <div class="col-sm-12 col-md-12 col-lg-4 col-xl-3">
                    {{ include('characters/_Card.html.twig', {'mode': 'Allprofiles'}) }}
                </div>
            {% endfor %}
        </div>
{% endblock %}